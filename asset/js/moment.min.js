<span class="hljs-comment">// Bahasa Indonesia</span>
moment().format(<span class="hljs-string">'MMMM Do YYYY, h:mm:ss a'</span>); <span class="hljs-comment">// September 4 2017, 10:53:16 pagi</span>
moment().format(<span class="hljs-string">'dddd'</span>);                    <span class="hljs-comment">// Senin</span>
moment().format(<span class="hljs-string">"MMM Do YY"</span>);               <span class="hljs-comment">// Sep 4 17</span>
moment().format(<span class="hljs-string">'YYYY [escaped] YYYY'</span>);     <span class="hljs-comment">// 2017 escaped 2017</span>
moment().format();

moment(<span class="hljs-string">"20111031"</span>, <span class="hljs-string">"YYYYMMDD"</span>).fromNow(); <span class="hljs-comment">// 6 tahun yang lalu</span>
moment(<span class="hljs-string">"20120620"</span>, <span class="hljs-string">"YYYYMMDD"</span>).fromNow(); <span class="hljs-comment">// 5 tahun yang lalu</span>
moment().startOf(<span class="hljs-string">'day'</span>).fromNow();        <span class="hljs-comment">// 11 jam yang lalu</span>
moment().endOf(<span class="hljs-string">'day'</span>).fromNow();          <span class="hljs-comment">// dalam 13 jam</span>
moment().startOf(<span class="hljs-string">'hour'</span>).fromNow();

moment().subtract(<span class="hljs-number">10</span>, <span class="hljs-string">'days'</span>).calendar(); <span class="hljs-comment">// 25/08/2017</span>
moment().subtract(<span class="hljs-number">6</span>, <span class="hljs-string">'days'</span>).calendar();  <span class="hljs-comment">// Selasa lalu pukul 10.53</span>
moment().subtract(<span class="hljs-number">3</span>, <span class="hljs-string">'days'</span>).calendar();  <span class="hljs-comment">// Jumat lalu pukul 10.53</span>
moment().subtract(<span class="hljs-number">1</span>, <span class="hljs-string">'days'</span>).calendar();  <span class="hljs-comment">// Kemarin pukul 10.53</span>
moment().calendar();                      <span class="hljs-comment">// Hari ini pukul 10.53</span>
moment().<span class="hljs-keyword">add</span>(<span class="hljs-number">1</span>, <span class="hljs-string">'days'</span>).calendar();       <span class="hljs-comment">// Besok pukul 10.53</span>
moment().<span class="hljs-keyword">add</span>(<span class="hljs-number">3</span>, <span class="hljs-string">'days'</span>).calendar();       <span class="hljs-comment">// Kamis pukul 10.53</span>
moment().<span class="hljs-keyword">add</span>(<span class="hljs-number">10</span>, <span class="hljs-string">'days'</span>).calendar();

<span class="hljs-comment">// Bahasa Jawa</span>
moment().format(<span class="hljs-string">'MMMM Do YYYY, h:mm:ss a'</span>); <span class="hljs-comment">// September 4 2017, 10:53:54 enjing</span>
moment().format(<span class="hljs-string">'dddd'</span>);                    <span class="hljs-comment">// Senen</span>
moment().format(<span class="hljs-string">"MMM Do YY"</span>);               <span class="hljs-comment">// Sep 4 17</span>
moment().format(<span class="hljs-string">'YYYY [escaped] YYYY'</span>);     <span class="hljs-comment">// 2017 escaped 2017</span>
moment().format();                          <span class="hljs-comment">// 2017-09-04T10:53:54+07:00</span>

moment(<span class="hljs-string">"20111031"</span>, <span class="hljs-string">"YYYYMMDD"</span>).fromNow(); <span class="hljs-comment">// 6 taun ingkang kepengker</span>
moment(<span class="hljs-string">"20120620"</span>, <span class="hljs-string">"YYYYMMDD"</span>).fromNow(); <span class="hljs-comment">// 5 taun ingkang kepengker</span>
moment().startOf(<span class="hljs-string">'day'</span>).fromNow();        <span class="hljs-comment">// 11 jam ingkang kepengker</span>
moment().endOf(<span class="hljs-string">'day'</span>).fromNow();          <span class="hljs-comment">// wonten ing 13 jam</span>
moment().startOf(<span class="hljs-string">'hour'</span>).fromNow();

moment().subtract(<span class="hljs-number">10</span>, <span class="hljs-string">'days'</span>).calendar(); <span class="hljs-comment">// 25/08/2017</span>
moment().subtract(<span class="hljs-number">6</span>, <span class="hljs-string">'days'</span>).calendar();  <span class="hljs-comment">// Seloso kepengker pukul 10.54</span>
moment().subtract(<span class="hljs-number">3</span>, <span class="hljs-string">'days'</span>).calendar();  <span class="hljs-comment">// Jemuwah kepengker pukul 10.54</span>
moment().subtract(<span class="hljs-number">1</span>, <span class="hljs-string">'days'</span>).calendar();  <span class="hljs-comment">// Kala wingi pukul 10.54</span>
moment().calendar();                      <span class="hljs-comment">// Dinten puniko pukul 10.54</span>
moment().<span class="hljs-keyword">add</span>(<span class="hljs-number">1</span>, <span class="hljs-string">'days'</span>).calendar();       <span class="hljs-comment">// Mbenjang pukul 10.54</span>
moment().<span class="hljs-keyword">add</span>(<span class="hljs-number">3</span>, <span class="hljs-string">'days'</span>).calendar();       <span class="hljs-comment">// Kemis pukul 10.54</span>
moment().<span class="hljs-keyword">add</span>(<span class="hljs-number">10</span>, <span class="hljs-string">'days'</span>).calendar();      <span class="hljs-comment">// 14/09/2017</span>
 
